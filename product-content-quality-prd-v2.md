# 产品内容质量升级 PRD：Resume 与 Cover Letter（整合版）

版本：V2.0 · 日期：2026-09-04 · 优先级：当前最高优先级

状态：整合需求稿，可用于产品/工程联合评审及 G0 基线工作。本文合并此前质量讨论、质量 PRD V1、旧流程 PRD 与 9 月 3 日交接记录；实现状态以第 1.2 节为准。开源基准版本、样本事实确认及发布阈值按第 11 节冻结后执行验收。本文不代表线上已经部署或质量已经达标。

## 1. 背景与目标

用户提供的满意简历 PDF 与产品生成的 Word 显示：相同的五段核心工作经历，在 Word 中全部被压缩成一条描述；具体行动、工具、协调规模和项目背景大量缺失，多段经历变成相似的行政支持概述。两份正文约 634 与 275 个英文词，但字数差不是质量判定本身。

目前不能断定缺失发生在原始输入、解析、筛选、写作还是审核阶段。满意 PDF 可能使用了额外补充材料，岗位也未必相同，不能将两份成品直接视为同条件实验，更不能将 PDF 中的所有事实自动写入用户资料库。

本期目标：让产品生成的 Resume 和 Cover Letter，在真实、完整的同一份候选人材料与同一 JD 下，达到经用户认可的 Career Ops 参考成品水平。交付物必须能够体现候选人的具体能力，并只需轻微措辞或个性调整即可用于申请；不要求逐字模仿或保证获得面试。

“轻微修改”指不需要用户重新补回系统漏掉的核心案例、不需要纠正虚构事实、不需要重写整段叙述。真实性、信息保留、岗位针对性、可读性和导出完整性共同决定质量。

### 1.1 与旧 PRD 的关系

本文件将内容质量升级纳入 P0，取代《产品优化 PRD（第二版）》中“本期不包含生成引擎及 PDF 内容质量”的范围限制。旧 PRD 的保存、快照隔离、归档以及有 Resume/JD 即可生成草稿等原则继续适用。

本期只调整支撑内容质量所必需的页面与流程。页面重构、归档打磨等不作为质量升级的前置条件。本文新增的“文档质量状态”与旧有 Application 进度状态分开管理。

在本文覆盖的需求内，以本稿为实施依据；质量 V1 为历史草稿，旧流程 PRD 未被本文覆盖的需求保留为独立待办。交接记录只用于确认实施基线，其操作建议不自动成为新需求。若旧 PRD 的导航设计与交接记录不同，沿用第 2.4 节较新的已确认交互，不为统一文档重新改版。

**与 Relevance 分级 + Timeline Integrity 专项 PRD 并存。** V2 不取代该专项 PRD：V2 负责整体内容质量、事实边界、版本及发布要求；专项 PRD 负责经历相关性分级、展示档位与时间线完整性的具体规则。专项文档中的 12 个月阈值、`full / condensed / timeline_only / hidden` 四档、连续 low-relevance 经历合并规则及 Test 1–7 均继续有效，不因 Q2 的概括表述而废止。Q2 的实现和验收必须同时引用专项规则；如两份文档出现实质冲突，应明确记录并确认，不以“V2 较新”为由静默覆盖专项规则。

### 1.2 已实现、待验证与待开发基线

来源：用户提供的 `job-application-assistant-handover-2026-09-03.docx`，以及 2026-09-04 本地代码核查。交接记录声明发布版本为 `80cb2ed`；本地后续提交包含 `670323b`、`852afb7`。这些记录不能证明 Render 当前实际部署版本，G0 需核实。

| 能力/问题 | 当前证据与状态 | 本期处理 |
|---|---|---|
| 多条职责合为一条记录 | 交接记录确认根因；本地 ckb.py 已按分别书写的职责拆分 | 保留修复，补测复合段落、换行和日期异常，不重复重写已有效路径 |
| 分行拆分后的信息保留 | 代码仍依赖日期行与换行识别；并非完整语义事实提取 | 用真实材料验证 Q1，确认遗漏再改 |
| 证据选择及描述上限 | 本地仍有默认十条证据预算，以及按经历分类的条数上限 | 按 Q2 验证重要事实损失，不能宣称已消除机械压缩 |
| Resume/Cover Letter 独立版式 | 交接记录报告已实现并视觉检查 | 保留，按 Q7 回归；不是内容达标证据 |
| Cover Letter 错误报错修复 | 交接记录报告已修复未使用证据 ID 造成的误报 | 防回归，不将其等同求职信写作升级 |
| Cover Letter 未选雇主与时态冲突结构检查 | 本地 `cover_letter_contract_issues` 已采用正文雇主名称匹配与已选雇主白名单，以及任期结束日期与动词形式检查；有对应自动化测试 | 保留现有机制，按 Q6 防回归，不重新设计；动词识别仍是启发式，不宣称覆盖所有英语句式 |
| 短简历诊断及证据审核 | 本地后续提交包含相关检查 | 校准是否准确识别系统遗漏；不是已通过成品验收 |
| 前后端测试与构建 | 交接记录报告 60 项后端定向测试、24 项前端测试和构建通过，并注明两个历史全量失败 | G0 重新确认适用版本与失败性质，不把历史测试结果写成当前通过 |
| 同条件 Career Ops 对照 | 未提供运行记录 | 建立两类文档基准、复用清单及盲评 |
| 少量补问、局部修复、状态闭环 | 本稿提出目标，未全面核实实现程度 | 工程逐项映射现有能力，优先补缺口 |

### 1.3 本次成品证据边界

满意样本：`cv-flora-zhong-randstad-v2-2026-09-04.pdf`；差样本：`Tailored CV (7).docx`。已比较正文并检查满意 PDF 的两页视觉效果；差样本 Word 未完成视觉渲染，因此现有比较支持内容差距结论，不支持该 Word 分页/排版缺陷结论。

交接记录还提及两份 Kwinana 参考文件，但本次未读取原件，不将其视为已验证的 Cover Letter 基准。测试材料保留在授权的私有环境，本文不复制联系方式或完整履历。

## 2. 成功标准与范围

### 2.1 本期包含

- 原始材料完整解析、经历事实保留及来源追溯。
- 岗位重点理解、证据选择与篇幅分配修正。
- Resume 与 Cover Letter 分别制定写作策略，共享一致事实。
- 对影响内容质量的缺失事实进行简短、可跳过的补问。
- Cover Letter 的申请角度与可靠岗位/公司背景使用。
- 事实审核、内容质量审核、有限修复及人工编辑后的复核。
- 主动更新申请材料、生成版本保留、失败恢复。
- DOCX/PDF 正文一致性、可读性及完整导出。
- Career Ops 能力复用评估、实际模型对照及真实样本发布验收。

### 2.2 本期不包含

- 自动投递、找工作、招聘联系人挖掘、面试辅导和薪资谈判。
- 全量复制 Career Ops 的 CLI、Dashboard 或所有技能。
- 强制重做整个前端、用户模型切换市场或模板商城。
- Selection Criteria 写作能力重构；既有可选功能继续保留并做防回归检查。
- 为追求好看而新增不存在的数字、成果、资历或工具经验。

### 2.3 当前默认假设

本期先覆盖现有产品的英文申请场景，默认沿用用户配置的 English variant；未配置时沿用产品现有默认值。具体字数是编辑指导，雇主明确限制优先；不强制所有用户拥有同样多的案例或数字。第 10 节数值为本稿提出的发布标准，产品评审冻结后执行，不声称已通过历史数据校准。

### 2.4 必须保留的既有产品行为

- Resume 必选；Cover Letter 默认选中、可直接取消；Selection Criteria 默认关闭，只由用户主动开启。JD 中提及 SC 时仅作提示，不自动选择。
- 有可用的 Application Resume Snapshot 与用户确认可用的 JD，即可生成草稿。职位名称、自动提取成功、先点击 Diagnose、匹配分数及可选文档均不得成为额外硬门槛。无法确认的标题/雇主可省略并提示，不以占位符冒充完成。
- 文本提取与链接导入分别处理失败。保留输入和已填写字段，提供手动编辑或粘贴 JD 的入口，不静默丢失数据。仅承诺经验证、公开可访问的链接来源。
- 已保存申请恢复 JD、简历快照、文档选择、历史文档和进度；非硬必填字段可以主动清空并保存。刷新后未完成任务显示未完成与重试入口，不能伪装为成功。
- 采用交接记录描述的左侧申请列表作为工作区导航，保留状态筛选及 Load more；不恢复旧 Dashboard Tracker，不为本期另做三工作区重构。
- Draft 放弃/删除在 Edit saved job details 中提供，确认删除关联数据后回到可新建申请的位置。不得仅为修复生成质量要求用户删除或重建申请。
- 既有 Archive/Restore、Applied 历史、Backup & Restore 行为不退化。永久删除保留既有确认、关联数据清理及权限约束；布局迁移不是本期前置工作。
- Format 在对应文档开启后展示，保留手动选择及既有组合限制，不自动改选、不影响草稿生成门槛。
- Resume 与 Cover Letter 保留各自视觉层级；Cover Letter 正文左对齐。技术信息默认折叠，页面只显示结论、影响与下一步。

上述为继承约束，不宣称每条已经在当前线上实现。G0 将其标为已实现/回归失败/待补齐；阻断正常生成或造成数据损失的缺口与质量修复同为 P0，其余交互优化后置。

## 3. 开源参考与复用原则

### 3.1 已核实的候选参考

本次找到并阅读的主要候选是 [career-ops-hq/career-ops](https://github.com/career-ops-hq/career-ops)，历史搜索入口包含 santifer/career-ops。另有 [poferraz/career-ops](https://github.com/poferraz/career-ops) 同名项目；尚未确认用户生成满意 PDF 时使用的是哪个仓库、提交、模型与个人配置。

候选项目的 [modes/pdf.md](https://github.com/career-ops-hq/career-ops/blob/main/modes/pdf.md) 包含基于简历源材料的岗位改写、经历条目重排、事实检查与结构化渲染；[modes/cover.md](https://github.com/career-ops-hq/career-ops/blob/main/modes/cover.md) 包含公司研究、申请角度、证据选择及草稿确认。本段描述仅指本次查阅的 main 内容，不构成固定版本的实现审计。

该候选仓库的 [LICENSE](https://github.com/career-ops-hq/career-ops/blob/main/LICENSE) 当前标为 MIT。实际复用前固定提交、核对被复制文件及字体等附属资源的许可，保留必要版权与许可说明。不能将“仓库开源”当作所有资源均可无条件复制的依据。

### 3.2 必须形成的复用清单

工程先评估已有能力，再选择直接复用、适配或保留本地实现。每项记录：仓库及 commit、具体文件、用途、依赖、许可、用户体验差异、验证结果。以最小有效改造为原则，不要求另建通用 Agent 平台。

| 能力 | 本期复用方向 | 产品适配要求 |
|---|---|---|
| 简历内容选择与岗位改写 | 参考 pdf 流程，核对其引用模块后选用 | 使用本产品用户材料；保留完整事实；不得导入示例人物经验 |
| Cover Letter 角度与证据组织 | 参考 cover 流程 | 合并重复问题，缺失动机可省略；不照搬固定多轮确认 |
| 事实检查与写作质量规则 | 评估源文件及本地 reviewer 后复用 | 同时检查虚构与有价值信息丢失；关键词检查不能充当完整事实核验 |
| HTML/PDF 构建 | 评估 pdf 流程引用的构建器及模板 | 通过样本验证后复用；保证与 DOCX 内容一致，模板不能反向压缩事实 |
| 公司背景获取 | 复用检索思路或已有能力 | 仅把可靠来源用于雇主背景，失败时按 JD 写作；不虚构公司问题 |

不能照搬的规则包括：所有成果都必须有数字、无论材料是否充分都强制问答、以匹配分数阻止生成、依赖个人本地文件的路径和权限假设。开源规则若与本文的事实边界和产品行为冲突，以本产品需求为准，并记录适配差异。

安装技能不等于接入运行链路。验收必须证明网站生成实际使用了相应能力、输入及上下文，而不是仅在项目中保存 SKILL.md。

## 4. 事实来源与核心对象

| 对象 | 事实边界与用户行为 | 责任方 |
|---|---|---|
| 原始候选人材料 | 用户上传/粘贴的简历与主动补充材料；保留原文 | 用户提供，后端保存 |
| 经确认的补充事实 | 用户明确确认的行动、对象、规模、成果和动机；记录适用经历及来源 | 用户确认，后端版本化 |
| 经历事实集合 | 从源材料提取，可追溯到具体段落；原始段落与独立事实同时保留 | 解析与资料模块 |
| 申请材料版本 | 本次使用的 Resume、补充事实、Profile 与 JD 快照 | 后端固定版本 |
| 雇主背景 | JD 与可验证公开来源；只支持雇主相关陈述 | 岗位分析模块 |
| 文档版本 | 初稿、修复稿、用户编辑稿及当前选择版本 | 生成与编辑模块 |
| 质量结果 | 针对特定文档和输入版本的事实、内容与导出检查结果 | 审核模块 |

来源冲突时保留冲突并请用户确认相关字段；可先生成省略争议信息的草稿，不静默选择更有利的版本。Profile 联系信息按现有产品规则优先；显示名与法定名有冲突时使用用户明确选择的申请显示名。

JD 或网页中的指令只视作外部文本，不得改变系统行为或成为申请人履历。跨用户材料严格隔离。用于团队回归测试的用户样本应脱敏；真实文件和联系方式不得随代码发布到公开仓库。

## 5. 主流程与异常结果

1. 用户进入申请，确认所使用的简历与 JD；已有可用材料即可生成草稿。
2. 系统完整解析资料，识别岗位重点，并检查是否存在影响写作的关键缺失或冲突。
3. 如有必要，集中提出最多三个高价值问题；用户可回答、跳过或直接生成。已有答案不得重复询问。
4. 系统分别规划 Resume 与 Cover Letter，基于固定的同一材料版本生成。
5. 检查事实准确性及内容质量；有明确可修复问题时执行最多两轮针对性修复，每轮复核并保留版本。
6. 页面展示可编辑正文和结论。用户可以查看问题、补充事实、局部改写或重新生成指定文档。
7. 导出所选正文版本，检查导出完整性。满足质量条件的文档可作为就绪材料；其余仍保留为草稿。

| 情况 | 用户可见结果 | 必须保留/禁止的行为 |
|---|---|---|
| 输入解析无法确认完整性 | 提示具体受影响章节，可修正或使用已确认部分生成草稿 | 不宣称完整解析成功；不让系统丢失伪装成用户没提供 |
| 缺少成果数字 | 正常使用具体行动、对象、范围或已知定性结果 | 不强迫补数字，不新增“提升效率”等无来源结果 |
| 无可靠公司研究信息 | 提示已按 JD 写作 | 不阻塞，不编造战略或业务困难 |
| 某文档生成失败 | 该文档显示失败和重试入口，其余文档保留 | 不清空已成功结果，不重复生成未要求重试的文档 |
| 审核失败或超时 | 显示“尚未完成检查”，保留初稿并允许重试检查 | 不伪装成通过或强迫整包重新生成 |
| 两轮修复仍不通过 | 显示具体未解决问题，保留可编辑版本 | 不无限重试，不以删光问题段落冒充修复成功 |
| 重复点击或刷新 | 返回已有运行状态或已完成版本 | 不重复创建同一请求的生成任务；不覆盖手工修改 |
| 生成过程中更新材料 | 当前任务按启动时快照完成，并标记结果基于旧材料 | 不混用前后版本事实；用户另行触发新版本 |

## 6. P0 内容质量需求

### Q1：完整解析，保留事实粒度

经历记录必须保留任职身份、可靠日期和原始描述；多行或一段中多个独立行动不能被提前概括成一句职责。每个事实可追溯到原文，不要求机械“一行一个事实”。结构化步骤不得因日期格式、换行或复合句丢失事实、混淆雇主。

系统需要区分三类不足：源材料缺失、解析/筛选损失、生成未充分利用。只有第一类才向用户索要新事实；后两类由产品修复。用原文到结构化结果的对照核实，不能单凭低字数就判定用户材料不足。

### Q2：按岗位和信息价值选择内容

先从完整事实中识别岗位重点及支持证据，再分配篇幅。证据记录数量不能直接成为描述条数上限；全局固定十条等现有限制不得造成相关事实系统性丢失。是否移除限制或改变结构由工程基于对照实验决定。

每个重点要求对应的最有价值事实，应被使用或有可解释的省略理由：重复、相关性低、事实冲突、用户排除或雇主严格篇幅限制。不得只因预算被通用职责占满而删掉独特案例。

相关经历可展开，次要经历可压缩。无关经历允许省略或在简短“其他经历”中保留连续性，不能创造虚假时间线。既不强制保留所有工作，也不通过隐藏经历暗示不存在的连续任职。

#### Q2.1：Relevance / Timeline 专项规则引用与验收

本节是专项 PRD 的引用入口，不是简化版替代规格。实施必须保留：

- **12 个月阈值**：计算对象、起止日期口径及边界包含关系以专项 PRD 原文为准，不改成模型自由判断。
- **四档展示**：`full / condensed / timeline_only / hidden` 的判定、保留字段与转换规则以专项 PRD 为准。现有代码中的 `promote / keep / compress / omit` 不自动等同于这四档，工程须核对映射。
- **连续 low-relevance 合并**：合并条件、时间范围和展示方式沿用专项规则，不将未任职间隔改写为连续就业。
- **Test 1–7**：逐项保留专项原始输入与预期结果，与本文 A01–A24 一起验收；不得用一个笼统的“时间线无误”测试替代。

资料状态：已从 2026-09-04 的《评估简历经历保留策略》历史任务中找回专项 PRD v1.1 正文。已确认：只有隐藏造成的连续空档 **大于 12 个月**才触发兜底，恰好 12 个月不触发；low-relevance 经历之间的真实间隔 **不超过 2 个月**才可归入同一占位组；检查窗口包含最近可见经历至本次 CV 生成日期；当前任职覆盖至生成日期；只修复隐藏造成的 artificial gap，真实 gap 与部分未覆盖区间保持原样。Test 1–7 与 v1.1 新增 Test 8–15 均纳入自动化或端到端验收。历史任务不是代码发布证据。

### Q3：Resume 写作要求

- 摘要说明职业定位及一至两个有来源的主要优势；不得把 JD 要求写成已具备的能力。
- 重点经历优先展示具体行动、对象、业务背景、工具、规模和已知结果；不要求每条凑齐全部元素。
- 同一段事实可以合理拆分或合并，但不能将同一行动改词重复充数。
- 必须体现不同经历的差异；不靠轮换 Provided/Supported 等动词掩盖内容重复。
- 保留“支持/协助/参与”所表达的真实责任边界，不能为了有力而升级为领导或主导。
- 允许不改变含义的专业转述和解释性组织；不得把一般协调升级为某具体方法论、系统熟练度或政府制度经验。
- 系统/工具、学历与证书按真实材料和相关性纳入；不因为未匹配某个 JD 关键词就自动删除。
- 对当前资深行政/项目支持基准，约两页为设计目标；不是统一 650 词的强制指标。初级或材料少的简历可更短。

### Q4：Cover Letter 独立写作要求

Cover Letter 使用完整候选人事实集合规划，不仅依赖已压缩的 Resume 正文。它应解释“哪些经历使我能够承担该岗位”，而不是将简历条目连接成段落。

默认结构为：直接的岗位适配开头 → 两至三个重点能力及具体案例 → 有依据的申请角度或可迁移能力 → 简短结尾。默认正文约 250–400 词、约一页；雇主规定优先，材料少时允许更短，不能补空话凑篇幅。

具体要求：

- 开头连接岗位和真实经历，避免泛化热情宣言与空泛自我评价。
- 案例包含“做了什么、涉及什么对象/背景、为什么与该岗位有关”；只在有来源时写结果和数字。
- 没有用户确认的动机，不编造热爱、长期关注公司或职业愿望；可用客观岗位适配开头。
- 公司专属陈述必须来自 JD 或可靠公开来源；拟议贡献明确为未来意向，不宣称了解企业内部问题或保证成果。
- 根据 JD 形成重点，而不是逐项复述所有要求。有独立 Selection Criteria 时，不把求职信写成第二份逐条回应。
- 转行/行业相邻经历可以解释迁移关系，不冒充直接行业经验。雇主要求回答的缺口如实响应；其余缺口不默认写成自我否定段落。
- 到岗时间、身份和联系方式使用确认信息；不得根据通知期推算并承诺日期。
- 草稿无需逐步强制确认才能生成；用户始终可以编辑、检查和导出草稿。

### Q5：少量补问与材料更新

每轮最多三个问题，优先补齐最影响目标岗位表达的内容。问题应指向具体经历，例如“这次访问涉及多少人、你负责哪些安排”，而非让用户填写大而全的 STAR 表。

已提供、已确认或已跳过且材料未变化的问题不重复提出。跳过后省略缺失信息，保留已有事实，不把占位符放进最终正文。

补充事实默认只应用于当前申请；用户可明确选择保存到 Master Resume 供以后复用。更新 Master Resume 后，既有申请不自动改变；提供“将最新简历用于此申请”，确认后创建新材料版本，并将原有文档标为基于旧材料。其他申请不变。

需要明确区分两种更新：解析算法升级时，可以基于该申请原有快照原文重新解析，不得偷偷换成最新 Master Resume；用户更换申请材料时，才创建新的材料版本。原始快照完整的旧申请应能直接重新生成，不要求新建 Job。若历史快照原文缺失或损坏，提示具体缺口，允许用户为当前申请补回原文，再生成新版本。

仅模板变化时，用户可从已选正文版本重新导出；内容策略变化时，用户主动重新生成才产生新正文。任何发布都不得静默覆盖历史文件或人工编辑。

### Q6：审核与有限修复

事实审核检查新增或夸大的数字、日期、工具、身份、职责及成果；内容审核检查关键事实利用、岗位针对性、经历区别、套话与重复。两者必须同时通过，不能以“没有编造”代替内容质量。

检查必须引用具体段落和来源或缺失事实；无依据的主观风格偏好只能作为建议。关键词覆盖、字数、动词频率属于辅助信号，不能独立决定优劣。

修复只处理明确问题，保留已经正确且有价值的细节。修复后同时检查新增事实风险和内容损失；如修复稿明显退化，保留较好的可编辑版本并显示剩余问题，不能自动发布退化稿。

#### Q6.1：已实现的 Cover Letter 结构检查（防回归基线）

以下两项已在本地运行链路实现，位于 `backend/app/cover_letter_plan.py` 的 `cover_letter_contract_issues`，由 Cover Letter reviewer 调用；本期保留并回归，不视为待重新设计的能力：

1. **未选雇主**：在信件正文中匹配候选人材料里的雇主名称，与当前计划的已选雇主白名单比较。不先判断句子“是不是就业叙述”，旁带提及也参与检查；采用完整词边界与最长名称优先，避免名称包含关系误报。称呼前与落款后内容不按正文处理。
2. **时态冲突**：结合提及雇主的权威任期结束日期与现在时动词形式检查，不退回固定短语枚举。回归包括不同动词、插入雇主短语、跨行名称、过去时、完成时、情态表达、当前任期及月/年精度边界。

实现边界：当前动词检查主要识别 `I / we` 的谓语形式，并排除已识别的过去时、完成时与情态表达；它不等于已实现任意主语、从句或“任何现在时动词”的完整语法分析。发现这些边界内的新漏检时，保留失败样例后作定点修复，不将未覆盖范围写成已完成。

对应回归入口为 `backend/tests/test_cover_letter_plan.py` 中的 `test_structural_employer_and_tense_checks` 与 `test_employer_whitelist_uses_body_and_longest_whole_name`。通过本地回归仅证明该版本的测试结果，不证明线上部署或整体成品质量已达标。

### Q7：两份文档与导出一致性

Resume 与 Cover Letter 中相同事实的姓名、雇主、日期、工具、数字、身份和责任边界一致；允许措辞和详略不同。不同文档可选不同案例，不强制两边逐项覆盖所有事实。

DOCX 与 PDF 从同一所选正文版本导出，不各自重新调用模型写作。格式可以不同，但不得丢条目、章节或日期；正文无内部 ID、审核备注和占位符。PDF 文本可选择与提取，布局不遮挡；DOCX 检查分页、段落和标题，不把文本抽取成功视作排版通过。

### Q8：模型与运行链路可验证

每次运行内部记录实际使用的 provider/model、回退情况、输入版本、提示词/策略版本和关键阶段输出。不能仅记录配置中首选模型。

先在相同完整材料、相同 JD 和同样补充答案下比较现有模型与候选配置。优先定位内容损失，再判断是否换模型。发布配置及自动回退配置分别通过质量验证；未通过验证的回退模型不得无提示地产出“就绪”材料，应保留为待检查草稿并说明生成服务发生变化。

## 7. 文档状态、Ready 与编辑

以下为产品语义，字段名称与实现复用由工程确定。生成任务状态与文档质量状态分开；质量状态以“过期 > 待检查 > 待完善 > 可使用”为展示优先级，旧问题仍保留在历史中。

| 文档质量状态 | 条件 | 允许动作 |
|---|---|---|
| 待检查 | 有正文但检查未完成，或正文已被用户修改 | 查看、编辑、重试检查、下载明确标识的草稿 |
| 待完善 | 存在明确事实或重大内容质量问题 | 编辑、补充、针对性修复、草稿下载；不得作为就绪材料 |
| 可使用 | 当前版本事实与内容检查通过，用户仍可作最终个人确认 | 正常导出；导出检查失败则该格式不显示成功 |
| 基于旧材料 | 所关联材料已更新 | 查看历史、下载标识为旧版本的文件、重新生成 |

草稿/旧版本的下载通过明确按钮和文件名标识，正式正文不插入调试说明。正式导出还必须通过该格式的完整性检查。事实问题不能靠点击“忽略”获得通过，需更正正文或确认来源后重检。

Application Ready 仅在当前选择生成的全部文档均为当前材料版本、质量可使用且所需导出检查完成时可标记；关闭某可选文档后，该文档不参与 Ready 判定。已 Applied 的历史进度不因后续编辑被自动改写，但新材料状态独立显示。

用户编辑后保留旧版本并使原审核失效；局部改写保留其他已接受段落。系统不得在后台覆盖用户编辑。整篇重新生成必须由用户主动触发，并新建版本。

## 8. 功能影响与工程交付

| 现有模块 | 需要核查/修改的行为 | 工程输出 |
|---|---|---|
| ingest / ckb | 原文完整性、复合段落拆分、事实追溯 | 原文到事实的对照验证 |
| job_model / evidence_matcher | 从职责识别岗位重点，避免漏掉非独立 requirements 段落 | 岗位重点与事实对应结果 |
| resume_plan / cover_letter_plan | 消除机械压缩、独立文档策略与省略理由 | 旧新规划对比 |
| ai / reviewer / quality | 写作、具体质量问题与有限修复 | 初稿与修复稿的事实保留对比 |
| generation_trace / main / models | 实际模型、快照、文档版本与质量状态 | 端到端可追踪运行记录 |
| exporter / ats_verification | 同源导出、格式完整性 | 两种格式正文及视觉检查 |
| Application Detail / Profile | 少量补问、更新当前材料、编辑与状态展示 | 最少必要页面变化 |

以上为责任范围，不要求新增同名服务。工程设计负责具体接口、存储结构、并发保护及依赖适配。最低内部运行记录应能还原：输入版本 → 解析事实 → 选中/省略内容及理由 → 初稿 → 审核/修复 → 用户选择版本 → 导出。沿用现有访问隔离与删除机制，删除申请时同步处理其生成追踪材料。

## 9. 核心验收用例

| ID | 给定与操作 | 必须满足的结果 |
|---|---|---|
| A01 | 原文一段中包含三个独立且相关的行动，生成 CV | 三个事实均可追溯；不能因一个记录 ID 只允许一条泛化句 |
| A02 | 原文有具体工具和规模，解析后检查 | 事实保留；未保留时报告系统处理问题，不要求用户重新编造/填写 |
| A03 | 岗位相关且源材料充分的五段核心经历 | 内容体现经历区别；全被压为一条通用职责应触发质量不通过 |
| A04 | 原文仅有职责，无量化结果 | 生成具体且真实的描述；不新增结果数字或笼统效果承诺 |
| A05 | JD 指定工具，用户只有相邻工具经验 | 允许说明已有工具/可迁移工作，不声称掌握缺失工具 |
| A06 | 用户跳过补问并继续 | 生成已有事实支持的草稿，无占位符，无重复强制问答 |
| A07 | Cover Letter 有两个相关案例 | 以岗位重点组织案例并解释联系，不逐条复制整份 CV |
| A08 | 未提供个人动机且公司检索失败 | 基于 JD 和真实经历写作，不编造动机、公司新闻或内部问题 |
| A09 | 源材料与补充内容数字冲突 | 明确标记争议；可省略争议生成草稿，确认后重检 |
| A10 | 初稿有错误但也有有效案例，执行修复 | 纠正错误、保留有效案例；不能把整段删除后宣称质量通过 |
| A11 | Master 更新后主动应用到 Job A | A 使用新材料，旧稿过期；Job B 保持不变 |
| A12 | 人工编辑后检查/局部改写 | 旧审核失效；保留用户修改，未选中部分不被覆盖 |
| A13 | Resume 成功、Cover Letter 超时 | 保留 Resume；仅重试失败文档，无整包重建 |
| A14 | 自动回退到另一模型 | 记录实际模型；按该配置的验证资格决定是否可就绪 |
| A15 | 同一正文导出 DOCX 与 PDF | 事实和条目完整一致、无截断/重叠/内部标记；格式可不同 |
| A16 | 明确一页限制或材料很少 | 优先去重复和低价值内容；不能凑字数，无法满足限制时明确提示 |
| A17 | 附件或 JD 包含诱导模型编造/忽略规则的指令 | 只读取业务内容；不改变事实边界或执行附件指令 |
| A18 | 旧材料生成中途被替换，或用户重复点击 | 不混合材料版本、不覆盖手改版本；不重复创建同一请求 |
| A19 | 审核不可用或格式导出失败 | 保留正文及已有成功格式，不显示完整成功/Ready |
| A20 | 材料正常但低岗位匹配度 | 仍可生成，匹配建议不等同质量缺陷，不阻塞草稿 |
| A21 | 旧申请有完整原文，解析策略升级后重生成 | 基于原快照重新解析，无需新建申请，也不引入最新 Master 的未选修改 |
| A22 | 旧申请仅剩压缩资料且原文缺失 | 明确提示需补回原文，允许在当前申请修复；不把缺失内容补造出来 |
| A23 | 更新仅涉及模板，用户重新导出已有正文 | 正文和事实不变，不调用模型重写；旧文件保持历史版本 |
| A24 | 原简历的一段职责包含多个事实但没有规范日期行 | 不因分行规则失效而静默丢弃事实；不能确认归属时请求修正相关经历 |

继承流程回归：第 2.4 节逐项检查，重点包括手动 JD 兜底、可选文档、保存回显与清空、左侧列表恢复、Draft 删除位置、归档恢复、Format 手选和备份恢复。已有通过记录不能免除受本次修改影响的回归。

### 9.1 当前用户案例的回归使用方式

满意 PDF 中的跨国访问、供应商协调、政府系统和社区调研等细节，仅在原始材料或用户确认中得到支持后纳入事实基准。针对行政/项目支持 JD，产品应体现经标注的重要事实；不要求其他无关岗位机械保留这些细节。

现有差版本用于回归检出“在材料充分且相关时，五段经历全部泛化”的问题，不作为通用最低字数模板。两份现有文档都不是生成输入的替代品；需补齐或重建经用户确认的共同输入。

## 10. “达到 Career Ops 水平”的发布验收

### 10.1 基准建立

固定上游仓库、commit、执行方式、模型、个人配置和交互答案。将双方可用材料保持一致：如果 Career Ops 获得额外回答、项目材料或研究信息，产品也获得同一信息；另行记录获取这些信息的交互成本。

采用两类对照：相同模型下比较流程贡献；采用最终候选生产配置比较用户实际成品。不要求为了公平而隐瞒更贵配置的成本，也不把模型差异误判为流程差异。

满意 PDF 为表达与排版锚点，不是完整的同条件基准。Cover Letter 当前尚无用户认可样本，必须在 G0 阶段用相同事实/JD建立参考信并由产品负责人认可，不能只通过 CV 测试就宣称两类文档达标。

### 10.2 最小样本集与方法

- 至少六组候选人材料 × JD：充分相关、多段相邻经验、材料稀疏、无数字成果、严格篇幅、政府与企业表达差异。允许组合覆盖，但不能全部来自同一个候选人；使用授权脱敏或明确标记的合成材料。
- 每组生成 CV 与 Cover Letter，各配置重复三次。初稿与修复后成品分别留存，不能只挑最好的一次。
- 运行前标注已确认事实、每份文档必须覆盖的岗位重点和关键事实；Cover Letter 只标注精选案例，不要求复述所有 CV 内容。
- 以隐藏生成来源、随机顺序的方式由两位评审独立评分，至少一位具备招聘或简历审阅经验。争议由产品负责人裁决并记录；模型评分仅辅助。

### 10.3 评分与门槛（本稿提议）

真实性为硬门槛：测试集不得出现未经支持的申请人事实、责任升级或把项目规模改写为个人成果。任何一例先修复，再重跑受影响场景；不能用平均分抵消。

其余维度各 1–5 分：事实与案例利用、岗位针对性、表达具体程度、结构与可读性、个人特色与文档分工。1 分为需整段重写；3 分为可理解但需实质修改；4 分为仅需轻微修改；5 分为事实清楚、重点充分且无需实质修订。无数值结果不扣分，少字或少条目本身不扣分。

发布需同时满足：

1. 经人工标注的必保关键事实，在目标文档中全部保留或有评审接受的明确省略理由。
2. CV 与 Cover Letter 分别计算，至少 90% 成品五维平均分不低于 4，且各维不低于 3；其余失败必须被质量检查正确留为草稿，不得误标可使用。
3. 至少 80% 的逐份对照被评为不低于已认可的 Career Ops 对应成品；两个文档类型分别达标，不互相平均抵消。
4. 事实、正文导出一致性、A01–A24 关键功能用例及受影响的继承流程回归通过；不能仅依靠模拟生成文本的单元测试。
5. 满意 PDF 对应的重建案例获用户认可；Cover Letter 基准案例也获产品负责人认可。

若尚未确定上游身份或无法复现参考运行，只能表述为“达到已认可样本标准”，不得对外宣称已验证 Career Ops 对等水平。

同时报告实际费用、完成时长、补问次数、重试率和首次可使用率。G0 测量后冻结可接受的成本与时延预算；不得为省成本默默切换到未达标模型。超过预算由产品决定优化或收窄上线范围，不降低事实标准。

## 11. 执行顺序、发布与待确认事项

| 阶段 | 必须交付 | 进入下一阶段的条件 |
|---|---|---|
| G0 基线与复用核查 | 核实线上实际版本、已实现能力清单、共同输入、CV/信基准、固定上游版本、复用清单、初始质量/成本记录 | 产品确认样本及阈值；区分旧文件/旧部署与新生成缺陷，无需等待 UI 改版 |
| G1 信息保留与规划 | 定位损失阶段，修复解析和机械压缩，展示前后对比 | A01–A05 与事实追溯通过 |
| G2 两类写作与补问 | 独立文档策略、事实补问及材料更新 | CV 和 Cover Letter 均有真实生成改善证据 |
| G3 审核与导出 | 有限修复、人工编辑保护、同源导出、旧申请恢复与状态 | A06–A24 及受影响继承流程通过，两类格式完成视觉检查 |
| G4 小范围发布 | 执行第 10 节盲评并灰度验证 | 所有发布门槛满足，实际生产配置与测试配置一致 |

先对内部/小范围用户启用新策略，保留旧文档不重写。发现事实错误、持续漏内容或导出缺损时暂停新策略并保留已生成版本；旧策略若仍有已知质量缺陷，只能作为明确的草稿回退，不能重新包装成已验证能力。修复后重跑受影响的事实和质量场景再恢复。

| 待确认事项 | 建议默认方向 | 责任/完成时点 |
|---|---|---|
| 用户实际使用的 Career Ops 仓库及版本 | 暂以已核实候选做复用研究，用户实际版本优先 | 产品+工程，G0 |
| 满意 PDF 的共同输入与细节来源 | 原始材料加经确认补充；不把成品视为事实证明 | 用户/产品，G0 |
| Cover Letter 认可基准 | 生成同条件参考信后由产品负责人确认 | 产品，G0 |
| 数值阈值、模型费用与时延预算 | 采用第 10 节门槛，结合初测明确预算 | 产品+工程，G0 |
| 被复用模块的固定版本与适配边界 | 以源文件核查和对照收益决定，不全仓搬运 | 工程，相关模块实施前 |

## 12. 本稿自审结论

本稿已把核心目标、Resume 与 Cover Letter 的独立需求、来源边界、生成与 Ready 的区别、版本更新、失败恢复及真实成品验收连接起来，可以进入产品与工程联合评审。

V2 增补了交接记录与本地提交基线、分行职责修复的适用边界、既有交互约束、旧申请重新解析/材料更新的区别，以及模板重导出与正文重生成的区别。未将已报告的测试或发布当成线上质量已验证，也未要求重新建设已有有效能力。

尚不能承诺“照此编码即达到 Career Ops 水平”：其可检验含义已经定义，但上游身份、共同输入、Cover Letter 参考与预算必须在 G0 落实。具体 API、存储迁移和依赖选择属于工程设计，不要求另写大型平台方案。本文只制定需求，未运行生成基准或实施产品代码。
